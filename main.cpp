#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cout << "Hello DSA done by sam!\n";
    return 0;
}
// ctrl+ ` =>new term
// ctrl+aln+N ->run code
//to run code-> g++ file.cpp -o run
./run
